{
  "name": "CyberSOC AI Workshop - Python + IA Cybersécurité",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "features": {
    "ghcr.io/devcontainers/features/sshd:1": {}
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y tcpdump libpcap-dev iptables net-tools curl unzip && pip install --upgrade pip && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-python.vscode-pylance",
        "charliermarsh.ruff",
        "github.copilot"
      ]
    }
  },
  "remoteUser": "vscode",
  "runArgs": ["--privileged", "--cap-add=NET_ADMIN", "--cap-add=NET_RAW"]
}